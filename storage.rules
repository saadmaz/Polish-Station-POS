rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // No app feature uses Cloud Storage; deny everything.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
